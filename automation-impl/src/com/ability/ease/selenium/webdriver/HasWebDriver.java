package com.ability.ease.selenium.webdriver;

import org.openqa.selenium.WebDriver;

/**
 * this interface represents a {@link WebDriver} container
 * 
 */
public interface HasWebDriver {
	public WebDriver getDriver();
}

